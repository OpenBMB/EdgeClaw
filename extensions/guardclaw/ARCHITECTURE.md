# GuardClaw 隐私保护机制 - 架构文档

## 概述

GuardClaw 是 OpenClaw 的隐私保护扩展，通过敏感内容检测和会话隔离机制，确保敏感信息仅在本地处理，不会发送到云端模型。

## 核心架构

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           用户消息                                        │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                     GuardClaw resolve_model Hook                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │  1. 检查会话是否已标记为私密                                        │   │
│  │  2. 如果否，调用本地模型检测敏感度                                   │   │
│  │  3. 根据敏感度级别决定路由                                          │   │
│  └─────────────────────────────────────────────────────────────────┘   │
└─────────────────────────────────┬───────────────────────────────────────┘
                                  │
                    ┌─────────────┴─────────────┐
                    │                           │
                    ▼                           ▼
┌───────────────────────────────┐   ┌───────────────────────────────────┐
│         S1 (普通内容)          │   │        S2/S3 (敏感内容)            │
│                               │   │                                   │
│  → 使用云端模型 (如 Claude)    │   │  → 重定向到 Guard 子会话            │
│  → 主会话处理                  │   │  → 使用本地模型 (Ollama)            │
│  → 响应直接返回                │   │  → 响应注入回主会话                  │
└───────────────────────────────┘   └───────────────────────────────────┘
```

## 敏感度级别

| 级别 | 描述 | 处理方式 | 示例 |
|------|------|----------|------|
| **S1** | 普通内容 | 云端模型 | 一般对话、技术问题 |
| **S2** | 中度敏感 | 本地模型 | 个人偏好、位置信息 |
| **S3** | 高度敏感 | 本地模型 + 隔离 | 密码、密钥、财务信息 |

## 会话隔离机制

### 子会话 (Subsession) 架构

```
主会话: agent:main:main
    │
    └── Guard 子会话: agent:main:main:guard
            │
            ├── 存储完整敏感消息
            ├── 存储完整响应内容
            └── 独立的会话历史
```

### 数据流

```
1. 用户发送敏感消息
   │
   ▼
2. GuardClaw 检测到 S2/S3 内容
   │
   ▼
3. 消息路由到 Guard 子会话
   │
   ├── Guard 子会话：存储原始敏感消息
   │
   ▼
4. 本地 Ollama 模型处理
   │
   ▼
5. 响应完成后
   │
   ├── Guard 子会话：存储完整响应
   │
   └── 主会话：注入占位符和响应摘要
       ├── 🔒 [Private message - processed locally]
       └── 🔒 [Response from local model] + 响应内容
```

## 会话状态管理

### 会话私密标记

文件: `extensions/guardclaw/src/session-state.ts`

```typescript
interface SessionPrivacyState {
  sessionKey: string;
  isPrivate: boolean;           // 会话是否已标记为私密
  highestLevel: SensitivityLevel; // 会话中检测到的最高敏感度
  detectionHistory: Detection[];  // 检测历史记录
}
```

### 状态持久化

- 会话状态存储在内存中 (`Map<string, SessionPrivacyState>`)
- 一旦会话被标记为私密 (S2/S3)，后续所有消息都使用本地模型
- 这确保敏感历史不会泄露到云端

## 核心组件

### 1. resolve_model Hook

文件: `extensions/guardclaw/src/hooks.ts`

```typescript
api.on("resolve_model", async (event, ctx) => {
  // 1. 检查会话是否已私密
  const isSessionPrivate = getSessionHighestLevel(sessionKey) >= "S2";
  
  // 2. 检测当前消息敏感度
  const sensitivity = await detectSensitivity(message, config);
  
  // 3. 如果敏感，返回本地模型 + Guard 子会话
  if (sensitivity >= "S2") {
    return {
      provider: "ollama",
      model: "llama3.2:3b",
      sessionKey: `${sessionKey}:guard`  // 重定向到子会话
    };
  }
});
```

### 2. 敏感度检测器

文件: `extensions/guardclaw/src/sensitivity-detector.ts`

使用本地 Ollama 模型分析消息内容：

```typescript
async function detectSensitivity(message: string): Promise<{
  highestLevel: SensitivityLevel;
  reason?: string;
}> {
  // 调用 Ollama 模型分析消息
  // 返回 S1/S2/S3 级别
}
```

### 3. Gateway 集成

文件: `src/gateway/server-methods/chat.ts`

```typescript
// chat.send 处理流程
1. 调用 resolve_model hook 获取模型和会话重定向
2. 如果有会话重定向：
   - effectiveSessionKey = guardSessionKey
   - originalSessionKey = 原始会话
3. 使用 effectiveSessionKey 运行 agent
4. 完成后，将响应注入回 originalSessionKey
```

### 4. 响应注入

完成处理后，自动将结果注入主会话：

```typescript
// 读取 guard 子会话的最终响应
const guardResponse = readLastAssistantMessage(guardSessionKey);

// 注入到主会话
appendUserTranscriptMessage({
  message: "🔒 [Private message - processed locally]",
  sessionKey: originalSessionKey
});

appendAssistantTranscriptMessage({
  message: `🔒 [Response from local model]\n\n${guardResponse}`,
  sessionKey: originalSessionKey
});
```

## UI 集成

### GuardClaw 状态指示器

当 GuardClaw 激活时，UI 显示状态指示器：

```typescript
// WebSocket 事件
{
  event: "guardclaw",
  payload: {
    active: true,
    level: "S3",
    model: "llama3.2:3b",
    provider: "ollama",
    reason: "Detected password in message"
  }
}
```

### 前端显示

文件: `ui/src/ui/views/chat.ts`

```
┌────────────────────────────────────────┐
│  🔒 GuardClaw Active                   │
│  Level: S3 | Model: ollama/llama3.2:3b │
└────────────────────────────────────────┘
```

## 配置

### openclaw.json 配置示例

```json
{
  "plugins": {
    "entries": {
      "guardclaw": {
        "enabled": true,
        "privacy": {
          "enabled": true,
          "localModel": {
            "provider": "ollama",
            "model": "llama3.2:3b"
          },
          "sensitivity": {
            "S1": { "model": "cloud" },
            "S2": { "model": "local" },
            "S3": { "model": "local" }
          }
        }
      }
    }
  }
}
```

## 文件结构

```
extensions/guardclaw/
├── README.md                 # 用户文档
├── ARCHITECTURE.md           # 本文档
├── package.json
└── src/
    ├── index.ts              # 插件入口
    ├── hooks.ts              # resolve_model hook 实现
    ├── config.ts             # 配置 schema
    ├── session-state.ts      # 会话状态管理
    ├── sensitivity-detector.ts # 敏感度检测
    └── local-model.ts        # 本地模型调用

src/
├── plugins/
│   ├── types.ts              # PluginHookResolveModelResult 类型
│   ├── hooks.ts              # runResolveModel 实现
│   └── guardclaw-events.ts   # GuardClaw 事件发射器
├── gateway/
│   ├── server-methods/
│   │   └── chat.ts           # chat.send 集成
│   └── server.impl.ts        # WebSocket 事件广播

ui/src/ui/
├── views/chat.ts             # GuardClaw 指示器组件
├── app-gateway.ts            # WebSocket 事件处理
└── controllers/chat.ts       # 聊天事件处理
```

## 安全考虑

### 数据隔离

1. **敏感消息** - 仅存储在 Guard 子会话中
2. **主会话** - 只包含占位符，不含原始敏感内容
3. **云端模型** - 永远不会看到 S2/S3 级别的内容

### 会话级保护

一旦会话中出现敏感内容：
- 整个会话被标记为私密
- 后续所有消息都使用本地模型
- 防止历史上下文泄露

### 本地处理

- Ollama 在本地运行
- 数据不离开用户设备
- 无网络传输敏感信息

## 限制和注意事项

1. **性能** - 本地模型响应较慢（10-20秒）
2. **质量** - 本地小模型能力有限
3. **检测准确性** - 敏感度检测可能有误报/漏报
4. **内存** - Guard 子会话会增加存储开销

## 未来改进

- [ ] 支持更多本地模型选项
- [ ] 改进敏感度检测算法
- [ ] 添加敏感词自定义配置
- [ ] 支持端到端加密的 Guard 会话
- [ ] UI 中显示 Guard 子会话历史的选项

